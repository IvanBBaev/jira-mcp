# Decision ledger

> Status: living document. Single source of truth for project decisions —
> ARCHITECTURE.md's ADR summary and IMPLEMENTATION-PLAN.md's standing
> decisions are pointers into this file. Append-only: a reversed decision gets
> a new row superseding the old one, never an edit in place.

## Accepted

| ID | Date | Decision | Rationale |
|---|---|---|---|
| D1 | 2026-08-07 | Custom server, not Atlassian Rovo MCP | Rovo (as of 2026-08) has no agile/changelog/worklog surface (~14 tools, no boards/sprints/backlog), only coarse read/write/search permission groups, no plan gate, remote-hosted. (Original "OAuth-only" argument retired: Rovo added API-token auth 2026-02-24.) |
| D2 | 2026-08-07 | Cloud-only v1, Basic auth (email + API token) | Covers the actual use case; DC/PAT and OAuth are additive later (see AUTH.md v2). |
| D3 | 2026-08-07 | Port servicenow-mcp's dark Jira client | Proven code: host resolution, retry policy, ADF helpers, tests. Pinned at servicenow-mcp@5acdcc7 (donors: facebook-mcp@6de6f87, tiktok-mcp@59c4ce0); porting from a later SHA requires re-checking the panel's donor findings. |
| D4 | 2026-08-07 | facebook-mcp skeleton + tiktok-mcp strictness | House template v2; best-of of both repos. |
| D5 | 2026-08-07 | Low-level SDK `Server`, not `McpServer` | Keeps the `structuredContent` envelope under our control; `registerTool`'s private zod validation throws `McpError`. |
| D6 | 2026-08-07 | `/rest/api/3/search/jql` only | Legacy search endpoints removed by Atlassian 2025-08-01. |
| D7 | 2026-08-07 | No delete-issue tool in v1 | Irreversible; revisit with the tiered gate in v2. |
| D8 | 2026-08-07 | Package name `jira-mcp-ai`, private `0.0.0` start | Mirrors `servicenow-mcp-ai`; publish decision deferred to Phase 5. Name verified free on npm 2026-08-07 (fallback: `@ivanbbaev/jira-mcp-ai`); reservation timing = open decision O-9. |
| D9 | 2026-08-09 | zod v3 (`^3.25`) for v1 tool schemas | Donor parity — all three donors ship zod 3.25.x, so ported assertion/schema idioms transfer verbatim; SDK ^1.30 peer range accepts v3 and v4. zod runtime usage is quarantined in `mcp/define.ts` (ARCHITECTURE.md), so a v4 migration is a bounded v2 task, not a rewrite. |
| D10 | 2026-08-09 | No dotenv dependency — env files load via Node's `process.loadEnvFile()` | dotenv ≥ 17 prints a stdout banner, which corrupts the MCP protocol on stdio; Node ≥ 22 ships `process.loadEnvFile`/`util.parseEnv` natively; tiktok-mcp already avoids the import for exactly this reason. One less runtime dep (supply chain). |
| D11 | 2026-08-09 | Doctor reports on **stdout**; structured logs stay on stderr | Doctor is a CLI run, not an MCP session — the stdout-purity rule protects the protocol, and no protocol is running under `doctor`. Exit codes and flags: AUTH.md §Doctor. |
| D12 | 2026-08-09 | Keep the donor's in-process telemetry counters; surface them only in `jira_capabilities` + doctor, export nothing | Counters answer "why is this slow / how often are we throttled" for free; an exporter would add a dependency, a config surface and an egress path to a tool whose whole premise is local-first. OBSERVABILITY.md §Counters. |
| D13 | 2026-08-09 | No circuit breaker in v1 | A single-user server never reaches the load where a breaker pays off, and a tripped breaker reads to a model as "the tool is broken". Replaced by: `upstream_degraded` after 3 consecutive failures + status-page remediation text. OBSERVABILITY.md. |
| D14 | 2026-08-09 | `plan_id`-bound apply (O-7 default taken at Phase 0.5 close) | Binding an apply to the plan it came from closes the window where a model re-invokes with drifted arguments and executes something the human never reviewed. In-memory, single-use, dies with the process. Contract: TOOLS.md §Plan-mode; CC-32. Owner may still override before Phase 4. |
| D15 | 2026-08-09 | Taint envelope on content-bearing reads — **one per result**, not per field | JSM portals and mail handlers let non-tenant users write into descriptions/comments, so Jira text is attacker-influenceable; branding it makes the confused-deputy risk visible to the model at ~one warning line per result. Per-field wrapping was rejected as budget-hostile. Port of facebook-mcp `mcp/taint.ts`. Contract: TOOLS.md §Untrusted content; CC-35. |
| D16 | 2026-08-09 | Worklog `started` offset comes from the **authenticated user's Jira timezone** (`/myself`, cached per process), falling back to the host TZ | Jira stores the worklog against the site's calendar; the host's TZ is an accident of where the agent runs (a CI box is UTC, the human is not). `Z` is never sent (CC-23); the offset source is injectable so tests pin it without touching the host clock. |
| D17 | 2026-08-09 | ADF traversal caps: `MAX_NODE_DEPTH = 64` (cycle/hostile-input terminator), distinct from the CC-09 list-indent cap of 6 | No spec pinned a node-depth cap; taken during WP-13. 64 is far above any document a human authors but bounds a hostile or cyclic tree; the indent cap stays a *rendering* rule, the depth cap a *safety* rule. Constants exported from `api/adf.ts`; owner may retune. |
| D18 | 2026-08-09 | Settings numeric ranges enforced at parse: timeout/budget 1..600000 ms, `JIRA_MAX_PAGES` 1..1000, port 1..65535; out-of-range → `invalid_number` startup finding, never a silent fallback | No spec pinned ranges; taken during WP-11. A typo'd `JIRA_REQUEST_TIMEOUT_MS=300000000` silently accepted would surface as a hung tool call — failing at startup names the actual mistake. Ranges live in `core/settings.ts` only (CONFIGURATION.md stays the naming/default authority). |
| D19 | 2026-08-09 | HTTP transport demoted to v1.5 (O-11 default applied at the Phase 2a start) — v1 is stdio-only; `JIRA_TRANSPORT=http` produces a startup error naming v1.5 | No concrete use case materialized by the decide-by point, and the http path drags in a token gate, loopback binding and session handling (CC-30) for zero current users. Settings keep parsing the http vars so the config surface is stable; only `mcp/transport.ts` refuses. Owner may still reinstate before Phase 4. |
| D20 | 2026-08-09 | Api-ring paging defaults to **one page** (`DEFAULT_SEARCH_MAX_PAGES` / `DEFAULT_USER_SEARCH_MAX_PAGES` / `DEFAULT_LIST_MAX_PAGES` = 1) and every paged result carries the `maxPages` it ran with | Taken during WP-20/21/22 — no spec pinned an api-ring default; one page keeps a single tool call cheap and predictable, callers opt into more. Carrying `maxPages` lets the tool ring tell "stopped at the page cap" (hint `max_pages`) apart from budget truncation (`_truncation`) instead of guessing from counts. |
| D21 | 2026-08-09 | User search: `MAX_USER_SEARCH_RESULTS = 100` clamp (CC-04 twin), and the assignable scope precedence is fixed client-side — when both `issueKey` and `project` are supplied, `issueKey` wins and exactly one param is sent | Taken during WP-21. Jira documents no cap and no precedence for `/user/assignable/search`; an unclamped `maxResults` invites budget blowups, and sending both scope params risks undocumented server behaviour. The narrower scope (issue) is what an assign flow actually needs. Wire pin: JIRA-API.md §Users. |
| D22 | 2026-08-09 | Wire-input hardening from Wave 2: agile ids validated as positive integers **before** any request; `MAX_MOVE_ISSUES = 50` cap on move-to-sprint/backlog refuses over-cap input with "Nothing was sent"; worklog `started` is emitted with a **colonless** offset (`+0530`) | Taken during WP-22/23. Agile routes 404 confusingly on malformed ids — rejecting pre-wire names the real mistake; the move cap matches Jira's own batch limit and a partial send would be an un-journaled half-write; the colonless offset matches the documented accepted format (JIRA-API.md §worklogs, D16/CC-23 detail — the injectable source may carry a colon, the formatter strips it). |
| D23 | 2026-08-09 | CC-34 has a single owner: `api/agile.ts` (`asAgileError`); `core/http.ts` stays root-agnostic — the dormant `apiRoot: 'agile'` branch was removed from `kindForStatus` | Two competing status mappings existed; only the api ring can tell a root probe (`/board` list → 403/404 means Agile unavailable → `unsupported`) from an id-bearing route (`/board/{id}` → the id may really be wrong → keep the kind, remediation names the licence possibility). The core branch was reachable by nothing in src and blanket-overrode the nuance. Full `describeStatus`/`kindForStatus` table unification stays a tracked Wave-4 item. Contract: CC-34. |
| D24 | 2026-08-09 | Registry/profile semantics: `reader` profile = `core,search,issues,meta,users,agile` with **readonly forced on the whole selection**; `lockProfile` is enforced in the registry (locked → a per-call profile is refused with `kind=config`, unlocked → injected); `plan_id` is Rng-issued (`plan_` + 24 hex) and stored with a `planFingerprint` hash of tool name + normalized args | Taken during WP-24/25 (D14 + O-6 refinements). Forcing readonly on the whole reader selection means adding a package can never silently re-arm writes; enforcing the lock at the registry seam makes it transport-independent; the fingerprint closes D14's drift window — an apply whose args changed since the plan fails `write_gated`, not silently executes. Contract: TOOLS.md §Plan-mode; CC-32. |
| D25 | 2026-08-09 | Doctor CLI rules: `--save` runs before any probe and short-circuits (no network); `--save` + `--json` is a usage error (exit 2); an unappendable `JIRA_JOURNAL_PATH` is a **fail** finding; an agile probe failure is a **warn**, never a fail | Taken during WP-25. `--save` is a config-write, mixing it with probes would network before consent; its output is human-interactive, so `--json` has nothing meaningful to emit; a journal that cannot be appended silently loses the audit trail the user asked for (fail); Agile being absent is a licence state, not a broken install (warn). Contract: AUTH.md §Doctor. |
| D26 | 2026-08-09 | **Gate B closed by defaults** at the Wave-3 open: O-3 `jira_update_comment` deferred to v1.5; O-4 backlog read deferred (JQL approximation documented in `jira_search`); O-5 assignable search = inputs on `jira_search_users` (no new tool); O-6 locked single profile per process = YES | The decide-by point arrived with no owner override recorded, so the ledger defaults apply (per §Open preamble). O-5 and O-6 are already implemented that way in Wave 2 (`api/users.ts` assignable variants; registry `lockProfile` enforcement — D24), so the defaults also match shipped code. Owner may still reverse any of these with a superseding row before Phase 4. |
| D28 | 2026-08-12 | Tools-ring dedup decisions from Wave 4 (WP-42): canonical handler guard lives in `mcp/tool-helpers.ts` (`guarded`) — a `JiraError` becomes the error envelope, **everything else rethrows**; the one bespoke catch (`jira_search`'s JQL-400 remediation rewrite, CC-05) goes through `rethrowUnexpected`; `jira_capabilities` keeps a local try/catch (sync handler, no I/O, cannot swallow a plan capture); annotation quadruples ship as five frozen consts checked against TOOLS.md; the D27 paging surface (`PagingInfo`/`pagingOf`) is hoisted generic over the stop-reason type so the mcp ring never names api types; package descriptions follow one style — noun phrase for the surface, dash, one operational fact — and `jira_capabilities` is titled "Describe this server" | Swallowing non-`JiraError`s would report a gate `PlanCaptured` as a tool failure — rethrowing is the only style that composes with the write gate; the registry already wraps handler throws, so end-to-end behavior is unchanged. Frozen annotation consts turn 11 hand-written quadruples into single-point-of-truth data the tests can diff against the spec table. |
| D29 | 2026-08-12 | Server-assembly decisions from Wave 4 (WP-40): the manifest snapshot (`src/tools/manifest.snapshot.json`) locks **structure only** — package/tool order, names, annotation quadruple, `writeTier`, sorted top-level input-schema keys as the client receives them (`toMcpTool`, not zod) — never titles/descriptions; regeneration is `UPDATE_SNAPSHOT=1` on the compiled test, resolving the source-tree path from `import.meta.url`; README tool tables are generated by `scripts/generate-readme.mjs` between `GENERATED:TOOLS` markers with a `--check` drift test that execs the real script inside the suite (`npm run readme` to fix); `main()`'s credential resolver applies profile fields **per-field** over top-level env, re-resolves a profile's site through `JIRA_ALLOWED_HOSTS`, and defers lookup so a broken inactive profile cannot block startup; `selectPackages` runs twice — full manifest to `buildServer` (gating stays in the registry), gated selection to the capabilities report | A snapshot that included prose would churn on every wording pass and train people to rubber-stamp regeneration; structural locking makes any diff a real surface change. Drift-checking README by running the actual generator means the test cannot pass while `npm run readme` would produce a different file. Per-field profile precedence matches how operators actually use profiles (override one credential, inherit the rest); routing the profile site back through the allowlist keeps profiles configuration, not an SSRF escape hatch. |
| D30 | 2026-08-12 | Status→kind mapping has a **single owner**: `errors.ts` `kindForStatus` (closes D23's tracked unification item). `http.ts` `describeStatus` delegates the kind and owns only remediation prose, falling back to the kind-keyed `REMEDIATION` map. Table fixes: 405 and 410 → `unsupported` (the removed 2025 legacy endpoints answer 410 — JIRA-API.md §Search; an invalid `nextPageToken` is 400, never 410); production now also gets 402 → `unsupported`, 408 → `timeout`, 501 → `unsupported`. CC-18 bug fixed on the way: `http.ts` treated mere **presence** of `X-Seraph-LoginReason` as a login denial, so a genuine permission 403 carrying `X-Seraph-LoginReason: OK` produced "regenerate your token" advice; the sole seraph rule is now `isLoginDenied` (value ≠ `OK`), pinned by an http-path regression test | Two diverging tables meant production (`describeStatus`) and the tested helper (`kindForStatus`) disagreed on five statuses, and the CC-18 header logic existed twice with only the unused copy correct. Retry semantics are untouched by design: the retry loop keys on the raw status before classification, so kind-table changes cannot alter replay behavior (JIRA-API.md §Rate limiting stays canonical). |
| D31 | 2026-08-12 | D16's per-process `/myself` cache is concretely a module-level `WeakMap<JiraRequestFn, MyselfResult>` in `api/users.ts`: filled on **success only**, no TTL, documented `refresh?: boolean` escape hatch | Keying on the request-function identity makes the isolation proofs structural: plan-mode capture wrappers and per-profile `withProfile` wrappers are distinct function objects, so a plan can never seed the live cache and profiles can never read each other's identity — no invalidation logic to get wrong. A failure is never pinned (success-only fill), and a process outliving a timezone change is out of scope for a session-scoped server. |
| D27 | 2026-08-09 | Tool-ring surface decisions from Wave 3 (WP-30…34): paged classic lists fetch **one page per call** and report loop state as `data.paging {pages, stopReason, partial, total?, nextStartAt?, note?}` (search: `nextPageToken`/`hasMore`) — never the `truncated` hint; `jira_list_fields` serializes the field catalog as rows + `duplicateNames {name, ids[]}[]` (the api's Maps are never emitted); partial createmeta returns renamed keys `fieldsSoFar`/`requiredSoFar` + `complete:false` + warning so a half list cannot be mistaken for the full one; `email_hidden` fires only when `includeEmail:true` was requested; `eventual_consistency` only when `reconcileIssues` was supplied; `jira_get_myself` drops `emailAddress`; `jira_get_sprint_issues` is content-bearing (branded untrusted, same shaper as `jira_search`); `sprint_move_required` fires on a create whose `fields` key matches `/sprint/i`; `jira_capabilities` ships as a factory (`corePackage(CapabilitiesSource)`, thunk re-read per call) reporting `CapabilitiesInfo` incl. `excludedTools` + `limits`, no counters until D12 lands | Taken during Wave 3 where TOOLS.md was silent or (untrusted enumeration) incomplete; two agents independently converged on the same paging shape, which is now pinned in TOOLS.md §Read shaping. Renamed partial-createmeta keys are deliberate model-safety: a scanner looking for `required` finds nothing rather than a plausible half list. Hint economy: hints fire only when the caller's own request makes them meaningful. Owner may retune any item with a superseding row. |
| D32 | 2026-08-12 | CC-02 lands as a session write registry (WP-51): `mcp/recent-writes.ts` keeps the numeric issue ids of **applied, successful** writes — last 50 (== `MAX_RECONCILE_ISSUES`), insertion-ordered `Set`, re-record moves an id to the back, `/^\d+$/` only (key-only write results record nothing — a post-write key→id lookup could only fail or lie). Recording is a server-assembly seam: `recordingGate` wraps **outside** `journalingGate` and records only when `executed && result.ok`, so plan captures record nothing by construction; the write side is injectable (`BuildServerDeps.recentWrites?`). `jira_search` auto-passes the registry as `reconcileIssues` **only when the caller supplied none** (`undefined` — an explicit `[]` is a caller decision and wins, like any caller list); `callerReconciled` keeps the `eventual_consistency` hint caller-triggered. **Partially supersedes D27**: `reconciledIssueIds` may now appear without the hint (auto-pass). The read side is the ambient module instance `sessionRecentWrites` — an accepted trade-off, not an oversight | Read-after-write is where eventual consistency actually bites an agent (create → immediately search → gone), and the fix must not require the model to remember to pass ids. Applied-successes-only keeps plan mode pure and failed writes silent. The ambient read seam is bounded and honest: `ToolCtx` is frozen and `searchPackage` is a plain const, stdio means one process = one server, and the *write* side stays injectable for tests; factory-izing the search package for one read would churn a frozen surface. |
| D33 | 2026-08-12 | D12's counters land as one injected `Telemetry` instance (`core/telemetry.ts`, created in `serve()`): increments live **only in `core/http.ts`** — `recordRequest` once per logical request, `recordRetry` per replay, `recordRateLimitWait` only in the 429 branch (always paired with a retry), `recordError` folding non-`JiraError` throws into `UNKNOWN_ERROR_KIND`; `telemetry` is optional on `createJiraRequest` (absent → the bare request function, zero overhead). Surfaced exactly twice, per D12: `jira_capabilities` merges `counters: telemetry.snapshot()` per call (sorted keys), and doctor renders a one-line counters row — **omitted** when the caller injected its own `jiraRequest`, because those counters would describe the injected fake, not the probes | Counting in the retry loop is the only place that sees every replay and every wait, so the numbers cannot drift from behavior; a second increment site is structurally impossible. Optional telemetry keeps the api-ring test fixtures untouched. The doctor omission rule prevents the one case where the counters would be actively misleading. No export path — local-first stays local (D12). |
| D34 | 2026-08-12 | The credential rule has **one home**: `core/credentials.ts`, a deliberately runtime-import-free leaf (type-only imports; host resolver injected). `src/index.ts` carries its single static-import exception and re-exports `buildCredentialResolver`; unknown `JIRA_PROFILE` **throws** (a typo must not silently fall back to default credentials); a profile's `site` is re-resolved through `JIRA_ALLOWED_HOSTS` (D29); lookup is lazy so a broken inactive profile cannot block startup. The third copy in `loadSettings` is folded via `CredentialSource = Pick<Settings, 'profiles' \| 'site' \| 'email' \| 'apiToken' \| 'activeProfile'>` — the Pick exists so the rule can run mid-load, before a full `Settings` value exists | Two earlier copies (doctor, resolver) had already diverged once; three was a bug factory. A leaf with no runtime imports is the only shape `index.ts` can statically import without dragging the module graph into the import-free entry, and load-time diagnostics computed by the same function as the per-call resolver can never disagree with it. |
| D35 | 2026-08-12 | `serve()` rebinds the **whole console** to stderr (`Object.assign(console, new console.Console({ stdout: stderr, stderr: stderr }))`) before the first dynamic import; CLI paths (doctor, `--help`, `--version`) never reach `serve()` and keep stdout. Pinned by an identity-polling `--import` probe test: the probe fires `console.log` only after observing the rebind, then asserts the line landed on stderr and every stdout frame parses as JSON-RPC | Our own source is console-free (eslint `no-console`), but a *dependency's* stray `console.log` would wedge prose between JSON-RPC frames — the one failure eslint cannot see. Rebinding before anything else loads closes the window completely; polling for the identity change makes the test deterministic instead of racing server startup. |
| D36 | 2026-08-12 | The gate hardens (WP-50/52): `npm run check` now also runs the tarball assertion (`scripts/check-tarball.mjs` — exact 85-file manifest, secrets/tests/maps excluded) and executes the test suite **under c8** (`test:coverage`) with `check-coverage` floors lines 94 / branches 82 / functions 97 / statements 94 (`.c8rc.json`); plain `npm run test` stays uninstrumented for quick loops; `npm run coverage` = build + `test:coverage`. The donor pins no statements floor — ours equals lines because tsc output makes them the same in practice; floors are a few points **under** the measured tree by design (a floor must not creep down, not a target to chase) | A tarball check outside the gate is a check nobody runs until publish day; coverage thresholds outside the gate are decoration. Folding both into `check` makes every finishing run enforce them, and keeping an uninstrumented `test` preserves the fast inner loop. |
| D37 | 2026-08-12 | Release engineering ships **inert** behind O-9/O-10 (WP-50): `publish.yml` publishes to npm via OIDC trusted publishing (`id-token: write`, no `NPM_TOKEN`) with `--provenance --access public`, guarded so nothing fires until the owner acts — `vars.PUBLISH_ENABLED == 'true'` (unset until O-9), a pushed `v*` tag, `prepublishOnly` = `npm run check`, and provenance itself failing while the repo is private (O-10) is the correct behavior, not a bug to route around; `server.json` is generated on the 2025-12-11 MCP registry schema with the plugin source `"./"` until an npm publish exists to point at | The whole pipeline can be written, reviewed and dry-run now while the *decision* to publish stays an owner gate — flipping one repository variable and pushing a tag is the entire go procedure, with no workflow edits on publish day. Trusted publishing removes the long-lived token class of supply-chain risk entirely. |
| D38 | 2026-08-13 | The v1.5 block of ROADMAP.md **graduates into committed scope** (Wave 6): the markdown ↔ ADF subset (headings, bullet/ordered lists, code fences with language, inline code, bold/italic, links, mentions) as pure converters in `api/adf.ts` plus optional `format: "markdown"` on the two content reads (`jira_get_issue`, `jira_get_comments`); the agile write set `jira_move_to_backlog`, `jira_create_sprint`, `jira_start_sprint`, `jira_close_sprint` (all standard write tier); the saved-filter reads `jira_list_filters` / `jira_get_filter`; and `jira_update_comment` (the D26 deferral matures). **Not graduating**: HTTP transport (D19 stands), backlog *read* (the JQL approximation of D26 stays), attachments/OAuth/DC (v2). Converters stay network-free — `@name` mention *resolution* is not a converter concern; write-side markdown (a `format` field on write tools) is a fast-follow once the parser has landed, not part of the agent scope | The roadmap's own rule: graduation requires a ledger entry. This is the natural next wave — v1 is feature-complete behind owner gates (O-9/O-10/Gate C), so the queue is either idle or v1.5; the selected items are the ones with zero new dependency surface and no new auth model. Keeping mention resolution out of the converter preserves the layering fact that only `core/http.ts` touches the network and converters are pure functions. |
| D39 | 2026-08-13 | The sprint lifecycle speaks **`POST /rest/agile/1.0/sprint/{sprintId}`** (Atlassian's "partially update sprint"), never the same-path `PUT`, and the `state: "active"` window (`startDate` + `endDate`) is validated CLIENT-side, per field, before any request. A stale-state 400 is rewritten in the api ring (`staleSprintState`): Jira's own words are preserved and only the remediation is replaced with a state-specific "re-fetch `jira_list_sprints`" (CC-36); a successful state change whose echo cannot be narrowed degrades to `{ sprintId, state, status }` with `sprint` omitted (CC-37) | The PUT is a documented FULL update — "any fields not present in the request JSON will be set to null" — so starting a sprint through it would erase the sprint's name and goal; the shape is easy to get wrong from memory, so it is pinned by a wire test. Jira's 400 for a missing window does not say which end is missing, and the hint vocabulary (`mcp/types.ts`) is closed with no code meaning "the sprint moved on", so the only honest place for that advice is the error's own remediation. The lenient echo is the one place strictness would actively lie: the write is already applied. |
| D40 | 2026-08-13 | `DESTRUCTIVE_WRITE_ANNOTATIONS` (`destructiveHint: true`) extends beyond `jira_update_issue` to **`jira_update_comment`** (whole-body replacement — CC-39) and **`jira_close_sprint`** (no reopen, unfinished issues moved out by board config); `jira_create_sprint` and `jira_start_sprint` are non-idempotent writes; both issue moves stay idempotent | The annotation exists so clients confirm exactly the calls where replace/end semantics can silently lose state. Closing a sprint stamps `completeDate` and cannot be undone through this API; editing a comment overwrites its entire body because Jira has no partial comment edit. Starting a sprint twice is a 400, not a no-op, so `idempotentHint: false` matches the transition precedent. |
| D41 | 2026-08-13 | Saved filters are **content-bearing**: both `jira_list_filters` and `jira_get_filter` brand their envelope (`_untrusted: true` + `untrusted_content`, one envelope per result); neither tool ever executes the stored JQL (no `run` flag, no `/filter/{id}/results`) — it reaches the search endpoint only through a deliberate `jira_search` call; and the api ring maps filters to a six-field allowlist **by construction** (`id`, `name`, `description?`, `owner? { accountId, displayName }`, `jql?`, `favourite?`) so `sharePermissions`/`editPermissions`/`subscriptions`/`sharedUsers` can never leak (CC-38, CC-40) | A saved filter's `jql` is tenant-user-authored text the model is *meant* to run next — the confused-deputy path in its purest form, and branding only the single-filter read would be dodgeable by listing instead. Keeping execution out of the filter tools preserves D6's single search path and keeps the brand between reading a third-party JQL string and running it. Building the output object (never spreading the wire row) means a field Jira adds later is excluded by default instead of leaked by default. |
| D42 | 2026-08-13 | The markdown dialect is **deliberately narrow and read-parity-bound**: the parser accepts exactly what the renderer emits (ATX headings, `-`/`1.` lists, fenced code, `code`/`strong`/`em`/`link`) — no tables, blockquotes, rules, images, reference links, setext headings, HTML, autolinks or two-space hard breaks; everything outside the subset degrades EXACTLY as `adfToText` does (CC-41) and the round trip is defined only up to a normal form (marks sorted, adjacent same-mark runs merged — CC-44). On the read side `format` is a rendering choice, never a shape change (CC-45): same wire bytes, same field set, same taint envelope; `raw: true` + `format` is refused rather than resolved | A parser that accepts more than the renderer emits invites markdown that silently loses meaning on the next round trip; what is not in the subset survives as text, which is always recoverable. Refusing `raw`+`format` keeps the reply independent of an ordering the caller cannot observe — silently picking a winner would hide the choice. Byte-identical default output means no existing caller can be broken by the feature's existence |
| D43 | 2026-08-13 | Converter security posture: `adfFromMarkdown` **never synthesises a `mention` node** (mentions render one-way — `@displayName` out, literal text back), and link marks are emitted only for `https?:` / `mailto:` hrefs — any other scheme (`javascript:`, `data:`, `file:`) drops the href and renders as text (CC-43) | Creating a mention needs an accountId lookup and converters are pure and network-free (D38); a literal mention syntax would let untrusted issue text that round-trips through the converter fabricate a notification to an arbitrary account — an impersonation primitive for zero benefit. The scheme allowlist means Jira-authored content cannot carry an executable URL into whatever renders the markdown |
| D44 | 2026-08-13 | Write-side `format?: "text" \| "markdown"` on every rich-text input (`description`, `body`, `comment` — all 7 write tools, uniformly): resolved in the **tool ring** (api builders keep taking `string \| AdfNode`), so `format: "markdown"` parses via `adfFromMarkdown` BEFORE the builder and plan mode captures the exact ADF an apply would send (CC-20). Refused only on the one contradiction — `format` alongside a raw ADF object (CC-46, validation at path `format`); `format` + `description: null` stays legal (null still clears — CC-31), `format` with an absent input is a no-op, omission is byte-identical to v1 `adfFromText` | Which grammar a string is written in is a tool-surface fact, not an api-ring fact — resolving it above the builder keeps `api/issues.ts` frozen and gives plan mode nothing to lie about. Refusing only the genuine contradiction keeps the field composable: a caller migrating to markdown does not have to special-case clears or tools without rich text. Uniformity across all seven tools means one rule to learn instead of seven |
| D45 | 2026-08-13 | A v2 subset **graduates into committed scope** (Wave 7): **attachments** — metadata on the issue read path, download into `JIRA_MEDIA_DIR` (unset ⇒ the binary tools refuse with `config`; Jira-supplied filenames are untrusted and are sanitized against path traversal), multipart upload with `X-Atlassian-Token: no-check`, size caps both directions — including the first extension of the `core/http.ts` request contract (binary response + multipart body), which stays the ONLY network-touching module; the **collaboration surface** — watchers list/add/remove, votes add/remove, components list/create/update, versions list/create/update, project role listing (standard tier, NO deletes); and the **irreversible write tier** — `jira_delete_issue`, `jira_delete_comment`, `jira_delete_worklog` behind `writeTier: "irreversible"`: refused unless `JIRA_ALLOW_IRREVERSIBLE=true` (blanket `JIRA_WRITE_MODE=apply` never covers the tier) and the plan captures a before-state snapshot of what the apply will destroy (facebook-mcp gate design; plan_id itself is already universal per D14). D7's v1 exclusion of deletes matures into this tier. **Not graduating**: OAuth 3LO and the Data Center adapter (live infra + owner auth-model decision), bulk operations | Same rule as D38: graduation is a ledger row. These are the remaining v2 items implementable offline — no new auth model, no new dependency, testable against fakes. Deletes were excluded from v1 because the standard gate was too weak a ceremony for unrecoverable operations; the tier IS that missing ceremony, so the deletes and the tier ship together. Extending the http contract in place keeps "only `core/http.ts` touches the network" true instead of inviting a second network path for binaries |
| D46 | 2026-08-13 | The operator allowlist (`JIRA_ALLOWED_HOSTS`) does **not** cover the attachment media host. Jira's 303 points at a signed, short-lived URL on an Atlassian media host that is not the site host and is not knowable in advance. What guards the hop instead: `?redirect=false` on the request so it usually never happens; `redirect: 'manual'` on every fetch so nothing is followed implicitly; `https` only; the SSRF blocklist (private and link-local refused); exactly one hop, from a binary GET only; and **no credentials on the hop** — the signature in the URL is the only credential the media host needs | Extending `JIRA_ALLOWED_HOSTS` to a host that cannot be known in advance would mean shipping a wildcard, which is strictly worse than a guarded anonymous hop. The allowlist keeps its meaning: it constrains which *Jira site* this server talks to |
| D47 | 2026-08-13 | `jira_download_attachment` is annotated read-only but **not idempotent**: `{readOnlyHint: true, destructiveHint: false, idempotentHint: false, openWorldHint: true}` — a bespoke quadruple; none of the frozen quadruples in `mcp/tool-helpers.ts` pairs read-only with non-idempotent. It mutates local disk, not Jira, and a second call leaves a second file (`renamed: true` reports it) | `readOnlyHint: false` was rejected on three grounds: it would force a `writeTier` where there is no Jira write to tier; it would hide the tool from read-only deployments that still want to fetch a file; and the plan gate only captures non-GET *requests*, so plan mode would execute the handler anyway — an honest annotation beats a gate that does not fire. `idempotentHint: false` is the literal truth |
| D48 | 2026-08-13 | `jira_upload_attachment` reads only a **plain basename inside `JIRA_MEDIA_DIR`** and refuses anything else instead of rewriting it: `../x`, `/etc/passwd`, `sub/dir/f` and backslash variants are refused with `kind=validation`; the store then re-resolves and re-checks the prefix as a second, independent lock. The seam type carries the property — the api ring hands the store a basename and never imports `node:fs` | Silently rewriting the model's `name` would upload a *different file* than the one requested, and accepting a path would turn a Jira tool into a general file-exfiltration primitive. Refusing names the mistake instead of hiding it |
| D49 | 2026-08-13 | A downloaded filename is **sanitized, never trusted, and never overwrites**. Jira filenames are tenant-authored text (D15): separators of both families, `..`, control chars, `NUL`, Windows-forbidden characters and trailing dots/spaces are stripped, Windows device names get an `_` prefix, the result is truncated to 120 chars keeping a short extension, an empty result falls back to `attachment`. The file is opened `wx` (never `w`), a collision uniquifies (`report-1.pdf`, up to 100 attempts, then `validation`), mode `0o600`. The untouched Jira filename is still returned as `filename` inside the taint envelope | A hostile filename is the attachment attack surface: traversal escapes the media directory, an overwrite lets one download clobber another. Sanitize-and-uniquify keeps every byte inside one flat directory while the model still sees the real name it asked for |
| D50 | 2026-08-13 | The whole `collab` surface is **standard tier with no destructive annotation**, including the two writes spelled "remove" and the ones that are a `DELETE` on the wire (`DELETE /issue/{key}/watchers`, `DELETE /issue/{key}/votes`): a watcher and a vote are LINKS, `jira_add_watcher`/`jira_add_vote` restore them exactly, and no issue content changes either way. Component and version **deletes are deliberately absent** from the package | D40's rule is that `destructiveHint: true` marks writes where replace/end semantics can silently lose state, not writes whose HTTP verb happens to be `DELETE`. Annotating a reversible link removal as destructive would train clients to click through the confirmation that `jira_update_issue` and `jira_delete_issue` actually need. Component/version deletes are a different animal — Jira rewrites every issue that referenced the deleted value — so they belong to the irreversible tier (D45), not to a package whose contract is "nothing here deletes anything" |
| D51 | 2026-08-13 | `jira_update_component` and `jira_update_version` are **partial** updates — only named fields are sent, an omitted field keeps its stored value — and an empty change is **refused locally with nothing sent** (D22) rather than dispatched as `PUT {}`. `description: ""` is the explicit clear; `null` is not accepted | These two `PUT`s are the exact opposite of `jira_update_issue`'s field-replace semantics (CC-31) and of `jira_update_comment`'s whole-body replacement (CC-39), so the difference is stated in the tool descriptions in those words. Sending `{}` would be a write with no intent behind it that still burns the plan/apply ceremony and still shows up in the journal; refusing it names the missing field instead. A one-way clear via `""` keeps the input a plain string and avoids a second null-vs-absent axis on a field that has no rich-text semantics to lose |
| D52 | 2026-08-13 | The **project reference is asymmetric and is passed through, not normalised**: `jira_create_component` takes the project **KEY** in the body, `jira_create_version` takes the **numeric projectId**; both list tools take either. The tool descriptions name the asymmetry and each other | Normalising would mean a hidden `GET /project/{key}` before every create — a fan-out the caller cannot see, an extra permission surface, and a second failure mode on the way to a write. Jira's own docs define these two bodies differently; surfacing that honestly costs one sentence in a description, hiding it costs a request per call and a class of "why did my create fail" that no error message can explain |
| D53 | 2026-08-13 | Component and version **descriptions are PLAIN TEXT**: no ADF, no `format` argument (D44 does not extend to this package), and version dates travel as **verbatim `YYYY-MM-DD`** calendar strings validated by regex, never as timestamps | These endpoints predate the v3 rich-text migration and store whatever string they are handed; sending an ADF document would store its JSON as literal text in a field a human reads in the project settings screen. Offering `format: "markdown"` on a field that cannot render markdown would be a promise the endpoint does not keep. A Jira version has no time of day, so accepting a timestamp would mean silently truncating it against an unknown timezone — rejecting it locally is the only answer that cannot be wrong |
| D54 | 2026-08-13 | A withheld watcher list is **distinguished from an empty one** on the wire: `WatcherList.watchersVisible: false` when Jira omits the `watchers` array, plus a plain-language `note` in the payload saying the list was withheld by permission and the issue must not be reported as unwatched (CC-47) | Jira answers a caller without "View voters and watchers" with a **200** carrying the count and no array — indistinguishable from "nobody is watching" to anything that reads `watchers.length`. The consequence is a model confidently telling a user nobody follows their incident. The fact is carried as prose in the payload rather than as a `Hint` because the hint vocabulary is closed (TOOLS.md §Hint catalog: "adding a hint code is a spec change here first") and no existing code fits — `truncated` means the character budget and tells the model *not* to page on, which is the wrong instruction. A `field_withheld` hint code remains a candidate if the catalog is ever reopened |
| D55 | 2026-08-13 | Project roles are **one tool with an optional `roleId`**, not a list tool plus a get tool: absent ⇒ `GET /project/{key}/role` flattened from Jira's name→URL map to `[{id, name}]` sorted by name; present ⇒ `GET /project/{key}/role/{id}` with the actors allowlisted to `{type, accountId?, displayName?}` | The two calls are never useful apart: the list endpoint returns **no membership at all**, and the id the drill-down needs exists only as the last path segment of the URL in that map — so any real question ("who administers ABC") requires both, in order. Exactly one request is issued per call either way, so the drill-down is not a hidden fan-out; splitting it would put a second tool in every model's context to serve one workflow. Sorting by name makes two calls against the same project produce the same order instead of inheriting JSON key order |
| D56 | 2026-08-13 | The irreversible tier's second gate is the **env var `JIRA_ALLOW_IRREVERSIBLE`, not a per-call confirm token** (the facebook-mcp donor's `FB_CONFIRM_TOKEN` design was considered and rejected), and the check sits **after the plan branch and before `plan_id` consumption**: planning always works, even on a server that will never permit the apply, and a config refusal does not burn the caller's single-use `plan_id` | A token in a tool argument is filled by the *model* — a ceremony it performs on itself that proves nothing about operator intent, which is exactly what the tier must establish; it would duplicate D14 (`plan_id` is already a per-call single-use token); and to be usable it would have to be printed where the model can read it, turning a secret into a constant. The env var is set by the human who starts the process and is invisible to the model — a different authority, which is the point of a second gate. The ordering means the operator can flip the variable and restart while the plan the model was shown is still the plan |
| D57 | 2026-08-13 | The delete plans carry a **before-state captured by the handler**, passed through a module-scoped `WeakMap<JiraRequestFn, BeforeSlot>` registered by `createPlanCapture` and written via `noteBeforeState(ctx.jira, snapshot)` — **no plan branch in any tool** (in apply mode the slot is unregistered and the same call is a silent no-op; the first note wins). A successful apply **echoes the same snapshot** in the receipt. Snapshots are allowlisted by construction (D41), excerpted (free text capped at 500 chars with a truncation flag, subtask keys at 20 with a count), pass the injected redactor, and every delete result is `_untrusted` (D15) | Returning it from the handler is impossible (plan mode throws `PlanCaptured` at the first mutation); putting it on `ToolContext` would add a mutable field every tool sees; re-reading inside the gate would double the reads and the gate does not know what entity a captured `{method, path, body?}` is about to destroy. The `WeakMap` is keyed by the seam instance, so a snapshot cannot leak across calls or sessions. Jira answers deletes with 204 and no body, and the journal line carries an `argsHash`, not values — a receipt of only `{deleted: true}` would be unauditable. A before-state is tenant-authored prose put in front of a model deciding whether to destroy it: the case the taint brand exists for |
| D58 | 2026-08-13 | All three deletes are annotated destructive **and `idempotentHint: false`** via a local `DELETE_ANNOTATIONS` quadruple in `tools/issues-delete.ts` rather than the shared `DESTRUCTIVE_WRITE_ANNOTATIONS` (which is destructive and idempotent) | The shared quadruple is right for a transition or a field overwrite and wrong for a delete: the second call answers 404, not 204, so "once" and "twice" have different outcomes. The house retry policy (JIRA-API.md §Rate limiting and retries) never replays an unsafe request precisely because it cannot tell those apart — claiming idempotence here would advertise exactly the replay that policy forbids |
| D59 | 2026-08-14 | Spec-doc status banners flip from "target-state spec (pre-code) — code may lag" to "normative and implemented — this document and the code ship together; drift is a bug"; `scripts/docs-lint.mjs` now enforces the opening each doc is allowed to claim (default plus `living document` for DECISIONS.md, `aspirational` for ROADMAP.md, `historical` for WORK-PACKAGES.md) and rejects the retired pre-code phrasing | With all 52 tools in 10 packages shipped and the gate green, "code may lag" was a false statement standing at the top of every normative document — the most-read line in the corpus. Fact-ownership tails are unchanged, so nothing about single-writer moves; making the banner a linted claim rather than decoration means it cannot silently rot back into a lie, and a copy-pasted old banner fails the gate instead of passing it |
| D60 | 2026-08-14 | `PlannedRequest` carries the redacted `query` alongside `method`, `path` and `body`: an optional object, redacted with the same redactor as the body, `undefined` entries dropped so it mirrors the URL that would actually be sent, absent entirely when the write carries no query. Additive — plan consumers that ignore it are unaffected | Plan mode's job is to show what apply would do, and for query-only writes there is nothing else to see: `DELETE /issue/{key}` differs from a subtask-tree deletion only by `deleteSubtasks`, and `DELETE /issue/{key}/watchers` names its victim in `accountId`. A plan showing method and path alone understated both |
| D61 | 2026-08-14 | Wave-8 release-audit follow-ups: the package locks its import surface before first publish (`exports` limits deep imports to the root entry, `types` names the declaration, `publishConfig` pins `--access public` + provenance), and the pinned registration example moves to `1.0.0` everywhere and becomes a linted mirror — docs-lint check 5 asserts README.md and the Pages page carry exactly CONFIGURATION.md's pin | The audit found the three Pages pins had silently drifted to `0.1.0`, a version that will never exist, because the single-writer check only scans `docs/**/*.md` — a copy-pasteable snippet that 404s is the worst possible first-run experience. And locking 88 deep-importable build files out of the semver contract is far cheaper before 1.0.0 than after |
| D62 | 2026-08-15 | `prepublishOnly` no longer runs the live advisory query: `check:publish` is the full gate minus `audit:prod`, `check` becomes `check:publish && audit:prod` (identical behavior for humans and CI), and `prepublishOnly` runs `check:publish`. Supersedes the `prepublishOnly` = `npm run check` clause of D37; every other guard in that decision stands | `npm audit` asks the registry what it knows *at the second it runs*, so inside `prepublishOnly` it could abort a release on a network blip or on an advisory disclosed in the minutes between the CI gate and the publish step — after the tag was pushed and inside the irreversible half of the release. That is not a property of the tarball being published; the tarball is byte-identical either way. The audit already runs on every PR, in every local `npm run check`, and as its own step in `publish.yml` *before* anything irreversible, so nothing is lost — a fresh advisory routes to a human deciding whether to ship rather than to a half-finished publish |
| D63 | 2026-08-15 | Wave-9 dependency triage, written into `.github/dependabot.yml` rather than a doc: `fast-check` 4 merges (dev-only, one consumer file); `@eslint/js` 10 is closed and re-cut as a paired `eslint` + `@eslint/js` major, with an `eslint` group so dependabot can never split that pair again; `@types/node` stays pinned to the `engines` floor (`^22`) with majors ignored; `typescript` `>=6.1.0` is ignored outright; `zod` 4 is a dedicated migration, not a merge. **Superseded in part by D82** — the `fast-check`, `@eslint/js` and `zod` clauses; the `typescript` and `@types/node` ones stand | Three of the five open PRs were unmergeable for reasons neither a red nor a green check explains. TS 7 fails at `npm ci` before reading a source file — it shipped without the programmatic compiler API that typescript-eslint's type-aware linting needs, and no release, canary included, accepts it. `@eslint/js` 10 *installs* green while leaving npm to nest a second copy, so the repo would lint with eslint 9 against eslint 10's ruleset. And zod 4 flips all 52 tools onto the SDK's v4 JSON-Schema converter, where the `strictUnions` this repo passes at `src/mcp/server.ts` becomes a silent no-op — a change the name-only manifest snapshot would very likely wave through. Encoding each verdict as an ignore with a named unblocking event keeps the queue honest instead of re-litigating the same three PRs weekly |
| D64 | 2026-08-15 | The redactor's depth cap is a stack-safety bound (128), not a log-field bound | The same walker serves log fields, tool results and plan previews. A cap tuned for metadata replaced ADF subtrees with `[MAX_DEPTH]`, which is data corruption in a result and an incomplete approval surface in a plan. Cycles are handled by the visited set and size by `JIRA_MAX_RESULT_CHARS`, so depth only has to clear `MAX_NODE_DEPTH` (64) plus the envelope |
| D65 | 2026-08-15 | The media store refuses a symlink whose target leaves `JIRA_MEDIA_DIR` | `stat` follows links, so the name check proved nothing about the bytes: a link left in the directory made `jira_upload_attachment` post any host file to Jira. `lstat` first, then `realpath` on both the link and the root — the root is itself commonly a link (`/tmp` → `/private/tmp` on macOS), and comparing a resolved target against an unresolved root would refuse perfectly legal files |
| D66 | 2026-08-15 | The taint delimiters are escaped inside the JSON they fence | `JSON.stringify` emits U+27E6/U+27E7 literally, so tenant text containing the closing delimiter closed the D15 block early and the rest of it reached the model as trusted server output. The escape is parse-identical, so the text↔`structuredContent` mirroring is unaffected |
| D67 | 2026-08-15 | Community-health layer for the public repo: `CONTRIBUTING.md` written from the tree (the `npm run check` chain, the network-fenced colocated suite, the layering zones, the normative spec corpus, both ledgers, and how to point the server at a real site safely), Contributor Covenant 2.1 as `CODE_OF_CONDUCT.md`, GitHub issue **forms** with blank issues disabled and security routed to `SECURITY.md` by contact link, a PR template that asks for the real gate output, and CodeQL as an **advanced-setup** workflow (`javascript-typescript` + `actions`, `security-extended`, SHA-pinned, no secret) rather than GitHub's default setup; `SECURITY.md` names private vulnerability reporting as the channel with an email fallback and promises no response time | A public repo whose contribution path is undocumented collects issues nobody can act on and PRs that miss the gate. The forms encode triage: the six facts that decide a Jira MCP bug are required fields, and the warning that Jira text and error bodies carry confidential data sits in the form where it is read rather than in a file nobody opens mid-report. Advanced-setup CodeQL keeps pins, permissions and schedule in review like every other workflow — default setup is a settings toggle that would silently disable the file — and scanning `actions` matters here specifically because `publish.yml` holds `id-token: write`. A policy promising an acknowledgement window an unfunded personal project cannot keep would be worse than none |
| D68 | 2026-08-15 | The Claude-plugin manifest registers the server through the pinned `npx` form owned by [CONFIGURATION.md](CONFIGURATION.md), not `node ${CLAUDE_PLUGIN_ROOT}/bin/jira-mcp-ai.cjs`, and the manifest joins README.md and the Pages page as a linted pin mirror; the plugin therefore starts working at first publish, not at install | A marketplace install is a git clone and `build/` is gitignored, so the shipped form could only ever reach the launcher's own error path ("run `npm run build` first") — it was broken for every consumer who was not a source checkout, and no manifest edit fixes that without committing build output. `npx` also makes the plugin consume exactly the pinned artifact the README and CONFIGURATION register, so there is one version-pin story instead of two |
| D69 | 2026-08-15 | A download that does not write every byte is a failure, not a result: `createNodeMediaStore.write` compares `bytesWritten` against the buffer length and removes the partial file before reporting, and every post-`open` failure now unlinks the file it created | The `wx` flag makes the name a claim staked at `open` time, so a failure after that point leaves a stub holding the name — and the name is all a later `read` has to go on, which is how a truncated file gets uploaded to Jira as the whole attachment. A short write is not a syscall error, so ignoring the return value reported a full-disk download as complete. The branch is asserted by construction rather than by a test: forcing a partial `write(2)` on a regular file needs a filesystem seam this store deliberately does not have |
| D70 | 2026-08-15 | Fixtures are recorded at the fetch seam by `scripts/record-fixture.mjs`, which wraps the injected `globalThis.fetch` from inside the real `api/*` call path and redacts in memory before the first write; `verify-live.mjs --record` keeps capturing above the mapper, and both stay | A fixture recorded after the mapper asserts `api/*` against its own output: the mapper drops a field, the fixture never carries it, and the guard is proven against fiction. Capturing under the mapper is the only way `fakeJiraRequest` holds Jira's shape rather than ours — and because the wrapper delegates to the fetch that was already installed, the recorder is not a second place that knows what a socket is, so the `core/http.ts`-only network rule still holds. Redacting before the write rather than cleaning after it is what makes a leaked token impossible instead of merely short-lived |
| D71 | 2026-08-15 | The recorder walks payloads itself and uses `core/redact`'s `redactString` per string rather than calling the object-level `redact()`, and it **drops** credential-bearing header keys instead of masking them | The generic redactor masks a value in place; a fixture needs three things it does not do — a placeholder that preserves the *shape* of what it replaced (an opaque `nnnnnn:uuid` accountId collapsed into the 24-hex form destroys what the fixture exists to preserve), a harvest pass that learns a tenant's display names and emails from the keys that name them and then removes them as literal needles anywhere including ADF prose, and removal of the key rather than the value, because the spec'd PII lint fails on an `Authorization`/`Cookie`/`set-cookie` **key** and a masked value under a live key would still fail the build. The depth cap is no longer part of this rationale — D64 raised it — but the other three reasons stand on their own |
| D72 | 2026-08-15 | Gate C is rehearsed offline first: `scripts/rehearse-live.mjs` drives the real `verify-live.mjs` against the stateful `scripts/fake-jira.mjs` over one pass per degradation it means to pin (the count lives in TESTING.md, which owns it). The fake is reached through the **supported** allowlist path plus a `dns.lookup` patch scoped to the spawned child, never by relaxing the SSRF blocklist and never by a test-only branch in `src/` | Each Gate C round trip costs an owner-provisioned tenant and a babysat run, so a driver bug that only shows up on the wire is the most expensive kind there is. The rehearsal found seven; two of them failed *after* mutating the project, which would have left junk on the scratch site. Reaching the fake by weakening `core/hosts.ts` would have made the rehearsal disprove its own premise — the blocklist is checked before the allowlist precisely so that no configuration can reach loopback, and a build with that check removed fails these assertions identically. Pass D exits 1 by design (an ambiguous write must not be replayed), which is why the runner asserts per-pass expectations rather than a bare exit code. A green rehearsal shortens the real-tenant list by zero items, and the runbook says so |
| D73 | 2026-08-16 | Gate C exercises the agile write surface (C27–C30) and accepts that it strands one sprint per `--write` run | The gate previously proved only agile *reads* (C11). `jira_create_sprint`, `jira_move_to_sprint`, `jira_move_to_backlog`, `jira_start_sprint` and `jira_close_sprint` would have been first exercised on a customer tenant. This server ships no sprint-delete tool on purpose, so the driver cannot clean up after itself; instead the sprint is named `gate-c-<runid> (safe to delete)`, is started and closed so it lands in completed history, and the run warns twice (claim note + stderr) with the manual UI removal steps. Adding a delete-sprint tool purely to service the gate was rejected — the gate does not get to widen the product's write surface |
| D74 | 2026-08-16 | A normative **test** tag must carry an explicit pointer — after the colon, either a `CC-nn` id or a `src/…/file.test.ts` path, comma-separated for several — and every `CC-nn` defined in CORNER-CASES.md must appear in a test **name** under `src/**`. Both are enforced by check 6 of `scripts/docs-lint.mjs`, which reads test sources so `npm run docs:lint` still needs no build. (This row states the rule without writing the tag out: check 6a cannot tell a claim from a description of one, so the corpus spells the syntax in exactly one place — the tag-definition bullet in docs/README.md, which the check exempts by name) | docs/README.md sold the tag as "asserted by a named test" and nothing verified it, so TESTING.md could specify a fixture PII lint and a `test/fixtures/` directory that did not exist and the corpus stayed green. The binding is explicit rather than inferred on purpose: fuzzy matching between prose and test names produces false confidence and false alarms in equal measure, and a check that is sometimes right makes a green run mean nothing. Binding to a test *name* rather than to file text is what makes a red run legible — `node --test` prints the name, so a failure says which corner case broke, and an id stranded in a comment above a deleted assertion cannot pass. The deliberate limit: a path pointer is checked for existence, not content — no regex can tell whether a file asserts an English sentence, and the CC form exists for claims that need the tighter binding |
| D75 | 2026-08-16 | The fixture PII lint is one exported detector (`src/testing/fixture-pii.ts`) driven from two directions — a walk over `test/fixtures/**` and a table of adversarial documents built inside the suite — and the recorder's redaction driver moves from the scratchpad into a compiled `src/testing/record-fixture.test.ts` that reaches `scripts/record-fixture.mjs` by a repo-root-relative dynamic `import()`; `test/fixtures/` is excluded from Prettier because the recorder owns those bytes | A lint proven only against a corpus that is nearly empty proves nothing, and the corpus stays nearly empty until Gate C — so the samples must run through exactly the code the corpus walk runs through, or the green tick is theatre. Two implementations of the same vocabulary is the point, not duplication: the recorder redacts on the way in and the lint detects on the way out, and the thing that fails the build has to be able to disagree with the thing that produced the file. The dynamic import weakens no layering zone (`import-x/no-restricted-paths` targets only `./src/core`, `./src/api`, `./src/mcp`) and needs no eslint override; a static import is impossible anyway because `scripts/*.mjs` is outside `rootDir` with no `.d.ts`. Prettier collapses short arrays while the recorder writes `JSON.stringify(doc, null, 2)` and asserts a re-record is byte-identical, so formatting a fixture would break the property the recorder exists to guarantee |
| D76 | 2026-08-16 | The media store's remaining uncovered arms are **kept and documented as unreachable**, not made testable: the short-write guard (D69), `writeFailure`'s `isJiraError` passthrough (reachable only through the short-write guard), `codeOf`'s two `unknown`-narrowing arms and the two `code === undefined` message arms that depend on them, and the `EISDIR` read arm. `EISDIR` is unreachable from the tool's own inputs — a directory is refused earlier by `lstat`/`!isFile()` with `kind: validation` — but it stays because it is the only thing covering the TOCTOU window between `stat` and `readFile` | Every one of these needs a filesystem seam the store deliberately does not have, or a thrown value Node's `fs` never produces. The alternatives are both worse than an uncovered arm: adding an injection point widens the media boundary that D45/D49 spent two waves narrowing, and a test that fakes reachability asserts the mock rather than the code. The precedent is D69 — asserted by construction, not by a test |
| D77 | 2026-08-16 | The claim "adding a hint code is a spec change in TOOLS.md first, then code" is **[honor]**, not a mechanized test claim; only the weaker claim that the table and `HINT_CODES` *agree* is mechanizable | A test can observe the state of two files, never the order in which they were edited. The tag was not merely imprecise — `HINT_CODES` appeared in zero test files, so the claim was backed by nothing while advertising a mechanical guard. Downgrading it is more honest than inventing a binding, and the reason is written inline in TOOLS.md so a later reader does not "upgrade" the tag back and re-create the false promise. The genuinely checkable half is now tracked as CC-72 |
| D78 | 2026-08-17 | The three distribution manifests are **checked against docs/CONFIGURATION.md by a test** (`src/manifest-sync.test.ts`), not generated from it: `server.json` must declare exactly the user-facing `JIRA_*` set with the documented defaults and requiredness, and the four manifest version fields must equal `package.json`'s. Two exemptions are named in the test with their reasons — `JIRA_LIVE_TEST` (test-only, never read by the server) and the `JIRA_PROFILE_<NAME>_*` family (no wildcard form in the registry schema) — and each is asserted to still be a documented row. `.claude-plugin/plugin.json` may surface `JIRA_MEDIA_DIR` as an optional field but must never surface `JIRA_ALLOW_IRREVERSIBLE` | `server.json` shipped 23 of 25 variables, and the two it dropped were the delete gate (D45) and the filesystem sandbox — the two settings whose absence is a safety story rather than a convenience one. Nothing read the file: not the build, not `docs-lint` (which touches `plugin.json` only for the install pin, D61/D68), not a test — while `src/env-docs-sync.test.ts` told readers in a comment that the manifest "is generated from the same table", which was never true. Checking beats generating here: the manifest carries registry-specific judgement a generator would have to encode anyway (`format`, `isSecret`, `choices`, the wording aimed at someone browsing a catalogue), and a check fails on the drift a generator would silently overwrite. The plugin asymmetry is the D45 line drawn where the user meets it: a media directory *narrows* what the server may touch and is unreachable from the plugin flow otherwise, while a delete gate a user can tick during install is not a gate |
| D79 | 2026-08-17 | A registered secret that would shred this server's own output is **warned about, never refused**. `core/redact.ts` exports `redactionRisk()` — a value shorter than `MIN_DISTINCTIVE_SECRET_LENGTH`, or one that is a substring of a fixed specimen of text the server always prints (log keys, event names, doctor's probe labels, `jira-mcp-ai`, `atlassian.net`) — and `core/settings.ts` turns a hit into a `warning`-severity `redaction_collision` finding naming the variable to edit, once per secret-bearing variable. `createRedactor` is unchanged: it registers what it is handed, and scrubbing is never weakened | Secrets are matched as literal substrings, so `JIRA_API_TOKEN=t` turns every diagnostic into a wall of `[REDACTED]` — including the output that would have explained why. The failure is safe (over-redaction, never under-redaction) but it is exactly what a first-time operator produces by pasting a placeholder. Making the redactor refuse such a needle was rejected: the operator set that variable believing the value is a secret, and a short-but-real credential (an older token, an operator-chosen `JIRA_HTTP_TOKEN`) would then be printed in full — a usability problem traded for a disclosure one. A credential-SHAPE test was rejected too: the space of valid credentials is not closed (API tokens old and new, OAuth, PATs), so "does not look like a token" is a false-alarm generator, and it answers the wrong question — the bug is blast radius, not pedigree. `warning` severity keeps the server starting, because a dummy token in a scratch config must not become a hard failure |
| D80 | 2026-08-17 | The tarball is compiled by `tsconfig.publish.json` (`sourceMap` and `declarationMap` off), reached only through `prepublishOnly` → `check:publish && build:publish && tarball:publish`; the development build keeps its maps, and `scripts/check-tarball.mjs --publish` fails on any shipped file that still carries a `sourceMappingURL`. `removeComments` stays **unset** | `files` excludes every map from the tarball because a map embeds absolute build paths and, with `sourcesContent`, the TypeScript source — but tsc writes the footer regardless, so every one of the 98 shipped build files pointed at a file nobody could fetch. The flags cannot simply move into `tsconfig.json`: `.c8rc.json` measures coverage over compiled output with `excludeAfterRemap`, so without maps the `src/`-written excludes stop matching and the floors stop meaning anything — the dev build must keep them. Splitting the config is the only form that satisfies both, and the checker asserts the config's shape in both modes so deleting it fails the everyday gate rather than release day. `removeComments` is rejected on measurement, not taste: it is one flag for both emits, and stripping ~199 kB of JSDoc from the `.d.ts` files this package advertises through `types` costs a consumer the hover documentation to save bytes nobody is paying for |
| D81 | 2026-08-17 | **Redact the object, then serialize** — for `doctor --json`, and for any future machine-readable output. The string redactor is a literal-text matcher with no notion of syntax, so applying it to a finished document can and does corrupt the document (`"ok": [REDACTED]rue`); deep-redacting the tree first reaches every string the report actually carries and cannot touch the punctuation. `--json` therefore writes through the raw stream sink, deliberately bypassing the string pass the human renderer uses. Redaction is not weakened to save the format (D79 stands): the output may be unreadable, but it is always parseable. Doctor's five remaining uncovered branch arms are **kept and documented as unreachable or untestable-by-design** rather than forced: two `error instanceof Error` narrowings for `catch (unknown)`, one `?? 'unknown'` that only a non-discriminated `EnvFileResult` requires, the `createJiraRequest` default (a test would be the one test in the file able to touch the network) and the `createReadlinePrompt` default (entering it reads the runner's stdin and never returns — which is why the factory is exported and tested directly over a `PassThrough` pair). Two arms that _were_ reachable-looking were instead deleted as provably dead: the host probe's `problems.length === 0` classification and the env-file probe's "environment was supplied directly" case. |
| D82 | 2026-08-17 | All three deferred dependency majors land **before 1.0.0** — `fast-check` 4 (2 call sites in `src/api/adf.property.test.ts`), `zod` 4 (`z.core.$strict` for the two `.strict()` return types in `src/mcp/define.ts`, two-argument `z.record()` in `src/tools/issues-write.ts`), and eslint 10 as the paired `eslint@^10.8.1` + `@eslint/js@^10.0.1` bump plus three `{ cause: error }` additions. The manifest snapshot is widened in the same pass to record every emitted JSON Schema, descriptions stripped (CC-82). Supersedes the `fast-check`, `@eslint/js` and `zod` clauses of D63; its `typescript` and `@types/node` clauses stand | D63's two blocking premises do not survive measurement. `@eslint/js` 10 does not "install green while nesting a second copy" — npm refuses the tree outright (`ERESOLVE … peerOptional eslint@"^10.0.0"`), and every eslint-adjacent peer already accepts `^10.0.0`, which is D63's own named unblocking event. (Its version still drifts behind eslint's inside the major — 10.0.1 against 10.8.1, because it is republished only when the shipped configs change — so the pair moves to its own latests, never to a matching number; the dependabot group comment says so where a maintainer meets it.) And `strictUnions`, which the SDK's v4 converter silently drops, was inert on this surface: the corpus has one union (`richTextArg`), both branches representable, and its emitted `anyOf` is unchanged. What zod 4 _does_ change is real, benign, and free only now — 28 of 52 `inputSchema`s change semantically while `additionalProperties: false`, `required`, the property-name sets and draft-07 stay byte-identical on all 52. After 1.0.0 that is a consumer-visible contract change; before it, nobody is watching. zod 3 is also a frozen line, so shipping 1.0.0 on it would pin a runtime dependency whose only security-fix path is a breaking upgrade (O-14, now closed) |
| D83 | 2026-08-17 | **Gate C is one command, and it refuses to run unnamed.** `node scripts/verify-live.mjs --project <KEY> --project2 <KEY2> --write --irreversible --confirm-site <host>` is the whole gate: the doctor preflight folds in as claim C00 and the residue inventory as C32, so ordering is a property of the code rather than of the runbook. `scripts/rehearse-live.mjs` runs that same command against `scripts/fake-jira.mjs` across eleven passes, leaving only the base URL and the credential to differ on the day (D72 still applies — re-run the rehearsal after any edit to the driver). A run that intends to mutate must name the host it is mutating (CC-83) and the gate reports its own litter rather than accumulating it silently (CC-85) | Gate C used to be a sequence a human held in their head at the end of a release — run `doctor`, then a read pass, then a write pass, then a delete pass — with the ordering living in prose and the cleanup living in a paragraph about a sprint. Every step someone has to remember at the end of a release is a step that gets skipped or reordered, and this is the one procedure here where "it only cost me a re-run" is false. The residue accounting was also simply wrong: D73 accepted one stranded sprint per run, but a `--write` run leaves five classes, including a `gate-c-<runid>` version that no tool in this server can delete. What stays removable is bounded by the product's own write surface and is not widened for the gate's convenience (D73) |
| D84 | 2026-08-17 | **An environment value still holding a client placeholder is a failure, not a value.** Doctor's `settings` probe fails on any `JIRA_*` value matching `\$\{[A-Za-z_][A-Za-z0-9_.:-]*\}`, printing only the matched placeholder (the redactor still applies) and skipping variables a startup finding already names, so no problem is reported twice. It is a widening of the existing probe rather than an eleventh one, because `docs/AUTH.md` numbers the probes 1–10 | Variables with a vocabulary or a format reject an unexpanded `${…}` themselves. The free-text ones — `JIRA_MEDIA_DIR`, `JIRA_JOURNAL_PATH`, and offline `JIRA_API_TOKEN` — accepted it, and `JIRA_MEDIA_DIR` then rooted the attachment sandbox at a *relative directory of that literal name*, under whatever cwd the client happened to pick (`settings.ts:401` unvalidated → `attachments.ts:563` checks only for empty → `resolve(dir)`), with `doctor` fully green throughout. The shape is not hypothetical: a plugin manifest that misspells `user_config` installs cleanly and hands the server six such literals (CC-90) |
| D85 | 2026-08-17 | **The redaction step fails closed.** `truncateResult` refuses with `unexpected_shape` when `redact` answers with anything that is not a result envelope, instead of falling back to the raw envelope; `registry.render` turns that throw into a complete `ok: false` result, so the caller still gets an answer. Alongside it, the truncation ladder gained an addressable root: a result whose `data` IS a string is now ellipsized at rung 3 rather than dropped at the floor. Four provably dead arms were deleted rather than tested — two `codePointAt(0) ?? 0` fallbacks in `result.ts` (string spread yields whole code points, so index 0 always exists; `length === 1` is the astral guard and the existence proof in one) and the generic transport-selection fallback in `transport.ts` (replaced by a `Record<Exclude<TransportKind, 'stdio'>, …>` table, making exhaustiveness the type system's job). Five arms in `http.ts` and seven in `tools/attachments.ts` are kept **uncovered and documented in place** | `redact` is typed `(value: unknown) => unknown`, so a redactor may answer with a depth marker, a string or nothing at all — and the old `isRecord(redacted) ? redacted : rawEnvelope` put the UNREDACTED envelope on the wire in exactly that case. The failure mode was "leak the thing redaction exists to remove, at the one moment redaction is known to be broken". Failing closed costs a result that was already broken and cannot leak. The root-setter bug had the same shape: a promise the ladder documents (rung 3 keeps a prefix) that one result shape could never receive, because the root visit passed a no-op callback. The kept-uncovered arms follow D81 — an arm is either reachable and tested, or unreachable and deleted, or kept with the reason written at the site: `attachments.ts` keeps seven because its store IS the `node:fs` code (a seam would move the subject under test into the double), and `http.ts` keeps a cap whose absence is an unbounded allocation |
| D86 | 2026-08-17 | **The first release is published with a token; every release after it is not.** Supersedes D37's "no `NPM_TOKEN`, ever" clause — every other guard in D37 stands. npm configures a trusted publisher on a package's settings page, and a package that has never been published has no settings page, so OIDC cannot publish the first version of a new name: the bootstrap is a granular access token in the `NPM_TOKEN` secret. `publish.yml` picks the credential by whether that secret exists rather than by an edit, so the steady state is reached by deleting the secret once the trusted publisher is registered, and a future release cannot silently keep using a token nobody meant to keep | The alternative bootstraps are worse: publishing the first version by hand from a laptop skips the gate, the tarball assertion and the tag/version agreement check that make a release reproducible, and publishing a throwaway placeholder version spends a version number on nothing and leaves it on the registry forever. A token that exists for one run and is then deleted is a smaller exposure than either, and the 90-day cap makes keeping it a chore rather than a decision — the deletion step is in RELEASING.md §5 next to the other post-publish work, where it is done while the release is still in hand |
| D87 | 2026-08-17 | **The first release is `0.9.0`, not `1.0.0`.** The surface is complete (52 tools, 10 packages) and the gate is green, but Gate C — a run against a real Atlassian tenant — has never happened, so every test in the suite answers a fake. The number says so. `1.0.0` is reserved for the version published after a live run, and RELEASING.md §6 keeps that run named as its blocker | Semver's promise is about compatibility, and a promise is worth what it costs to keep. Under `1.0.0` anything the first live tenant contradicts — a field shape, an endpoint's actual pagination, a permission error nobody could fake — has to arrive as a breaking major three days later, which teaches readers that this project's majors mean nothing. Under `0.9.0` the same discovery is a minor, and the pre-1.0 number is a standing, machine-readable statement of exactly the thing the docs otherwise have to say in prose. It also costs nothing that matters: the package installs, resolves and pins identically either way, and shipping it is what creates the npm settings page that D86's token exists to reach |

## Open (owner)

Defaults apply if no decision is recorded by the decide-by point. Full context:
`reviews/2026-08-07-extended-panel.md` §E-P3.

| ID | Decision | Default | Decide by |
|---|---|---|---|
| ~~O-1~~ | Initial commit + private `IvanBBaev/jira-mcp` remote (**Gate A** — blocks all agent fan-out) | **Resolved 2026-08-17:** the initial commit exists and is pushed. The remote is **public**, not private — O-10 resolved that way first and supersedes the clause here | — |
| O-2 | Scratch Jira site provisioning (**Gate C**) | Owner console task, checklist in Phase 0 | before Phase 1 exit |
| ~~O-3~~ | `jira_update_comment` in v1 | **Resolved 2026-08-09 by default → D26** (defer to v1.5) | — |
| ~~O-4~~ | Backlog read in v1 | **Resolved 2026-08-09 by default → D26** (defer) | — |
| ~~O-5~~ | Assignable search as inputs on `jira_search_users` | **Resolved 2026-08-09 by default → D26** (IN, no new tool) | — |
| ~~O-6~~ | Locked single profile per process | **Resolved 2026-08-09 by default → D26** (YES) | — |
| ~~O-7~~ | `plan_id`-bound in-session apply | **Resolved 2026-08-09 by default → D14** | — |
| O-8 | Journal content: redacted fields vs args-hash | Minimized hash form + rotation | before Phase 4 |
| ~~O-9~~ | npm name: reserve early vs wait + accept fallback scope | **Resolved 2026-08-17: publish.** The owner created the `NPM_TOKEN` secret and asked for the release to be prepared, which decides both halves at once — the name is taken by publishing under it rather than by reserving a placeholder, so the fallback scope (`@ivanbbaev/jira-mcp-ai`) is only needed if `npm publish` answers 403. Mechanism in D86 | — |
| ~~O-10~~ | Repo visibility | **Resolved 2026-08-15: public.** CodeQL ships as an advanced-setup workflow (D67); secret scanning + push protection, private vulnerability reporting and Dependabot security updates are repository settings, not files, and are still off | resolved |
| ~~O-11~~ | HTTP transport in v1 | **Resolved 2026-08-09 by default → D19** (stdio-only v1) | — |
| O-12 | Unattended (cron/CI) use as v1 acceptance criterion | Sizes priority of ops items F1–F3 | Phase 0.5 |
| O-13 | Anthropic-terms context (consumer vs Team plan) for the data-handling text | State the actual plan in the doc | Phase 0.5 |
| ~~O-14~~ | zod 3 → 4 before or after 1.0.0 (D63) | **Resolved 2026-08-17: before, and done (D82).** zod 3 is a frozen line — last stable 3.25.76 on 2025-07-08 — so 1.0.0 would pin a runtime dep whose only security-fix path is a breaking upgrade. The unbounded half, re-validating the emitted `inputSchema` of all 52 tools, is no longer a manual job: the manifest snapshot records them (CC-82) | resolved |
