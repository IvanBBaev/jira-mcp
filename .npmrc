# Fail in-repo installs on a Node runtime below the `engines` floor. This binds
# only contributors installing inside this repo; consumers get at most an
# EBADENGINE warning, so bin/jira-mcp-ai.cjs is the real consumer-facing guard.
engine-strict=true
